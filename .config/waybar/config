{
  "layer": "top",
  "height": 45,

  "modules-left": ["custom/launcher", "cpu", "memory", "disk", "tray"],
  "modules-center": ["hyprland/workspaces"],
  "modules-right": ["custom/updates", "custom/weather", "pulseaudio", "clock"],

  "pulseaudio": {
    "tooltip": false,
    "scroll-step": 5,
    "format": "{icon}   {volume}%",
    "format-muted": "   Muted",
    "format-icons" : {
      "default": ["", "", ""]
    }
  },

  "disk": {
    "interval": 60,
    "format": "󰋊 {free} ({percentage_free}%)",
    "path": "/"
  },

  "tray": {
    "icon-size": 18,
    "spacing": 10
  },

  "clock": {
    "interval": 1,
    "timezone": "Europe/Paris",
    "format": "  {:%H:%M    %d/%m}"
  },

  "cpu": {
    "interval": 15,
    "format": "  {}%",
    "max-length": 10
  },

  "memory": {
    "interval": 30,
    "format": "  {}%",
    "max-length": 10
  },

  "custom/updates": {
    "format": "{} update(s)",
    "exec": "paru -Quu | wc -l",
    "interval": 15,
    "on-click": "kitty -e paru && notify-send 'The system has been updated!'"
  },

  "custom/launcher": {
    "format": "",
    "on-click": "wofi",
    "on-click-right": "killall wofi"
  },
  
  "custom/weather": {
    "format": "{}°",
    "tooltip": true,
    "interval": 3600,
    "exec": "~/.config/waybar/scripts/wttrbar --location Gif-Sur-Yvette",
    "return-type": "json"
  }
}